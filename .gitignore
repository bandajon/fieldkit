__pycache__/
*.pyc
recordings/
# extraction trees from the removed pull feature still exist on deployed nodes —
# keep ignoring them so a git add -A there never stages footage
ingest/
# generated from config.yaml; contains camera passwords
go2rtc.yaml
# YOLO weights auto-downloaded by the Monitor detection overlay
*.pt
# detection training samples, operator-labeled on the Label tab
dataset/

# operator config: copied from config.example.yaml on first boot, holds field passwords
config.yaml
# live recording sessions, rewritten by the recorder so restarts resume them
record_state.json
# ops console: enrollment token hashes, fleet history, audit trail
ops_state/
# ops console config: copied from ops_config.example.yaml on first boot
ops_config.yaml
dist/
.venv/
