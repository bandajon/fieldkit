fastapi
uvicorn
requests
pyyaml
websockets
boto3          # Cloudflare R2 offload/mirror + dataset sync
# ultralytics  # optional — Monitor detection overlay (vehicle boxes + counts)
# pillow  # optional — Monitor detection overlay
